'''
可以使用len函数求出字符串的长度。len('abc')的值是3.

可以使用索引从字符串提取单个字符。在Python中，所有索引都从0开始。
Python使用0表示字符串中第一个元素，长度为3的字符串最后一个元素的索引值是2。
可以使用负数表示字符串从末尾开始索引。例如，'abc'[-1]的值是'c'

可以使用分片操作从字符串中提取任意长度的子串。如果s是字符串，那么表达式s[start:end]就表示s中从索引start开始至索引end - 1结束的子串。
例如，'abc'[1:3] = 'bc'
'abc'[0:len('abc')] = 'abc'

Python中有一个input函数，可以直接接受用户输入。用户输入的信息被看做一个字符串，并成为这个函数的返回值。


'''